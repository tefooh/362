{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the project-362 Tauri example: core defaults + every project-362 plugin command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "project-362:default"
  ]
}
